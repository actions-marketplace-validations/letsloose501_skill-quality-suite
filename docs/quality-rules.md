---
title: "Quality rules - every check an Agent Skill validator runs"
description: >-
  The full rule list of skill-quality-suite: specification conformance, structure,
  instruction quality, cross-harness compatibility, security, publishing and evals,
  each with its reasoning, its fix, its detection confidence and its false-positive
  risk.
---

<!-- generated by scripts/build_docs.py - edit that, not this -->

# Quality rules

Every finding the suite can emit, all 72 of them, rendered from `scripts/rules.py`.

`sqs.py explain <CODE>` prints the same reasoning at the terminal, and `sqs.py rules --module security` lists one module.

Each rule carries two gradings that are about **the check**, not about the skill:

- **confidence** - how reliably the check finds the thing it names. A filesystem fact or a parse is `high`; a regex over prose is `medium`; a similarity heuristic is `low`.
- **false positives** - when the thing *is* there, how often it is nonetheless intended. `README.md` inside a skill folder is a finding and also exactly what a repository-shaped skill does.

`--min-confidence high` keeps the facts and drops the heuristics, which is the gate you can leave switched on in CI.

## compat (CPxxx)

Whether the skill will work on somebody else's harness. `UNKNOWN` is never `NOT_SUPPORTED`: where a project's documentation is silent, the answer is unknown rather than an invented incompatibility.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `CP001` | warning | high | low | no | Field value the runtime cannot read |
| `CP002` | warning | high | low | no | Field rejected by the strict reference validator |
| `CP006` | warning | medium | medium | no | Feature that does not travel |
| `CP007` | info | high | low | no | Undocumented on a target harness |
| `CP008` | error | high | low | no | Target harness refuses the skill |
| `CP009` | warning | high | low | no | No adapter for the named harness |

### CP001 - Field value the runtime cannot read

**Why it matters.** The field is read; the value is not one of the ones it accepts, so the runtime falls back to its default and says nothing.

**Fix.** Use one of the accepted values.

### CP002 - Field rejected by the strict reference validator

**Why it matters.** `skills-ref` refuses unknown top-level fields. Real clients warn and load anyway, so the breakage only shows on publication.

**Fix.** Nest it under `metadata:`, which is the spec's escape hatch.

### CP006 - Feature that does not travel

**Why it matters.** The feature is real on one harness and inert or absent on another target. Nothing fails loudly: the instruction simply has no effect there.

**Fix.** Keep it if that harness is the only target; otherwise state the behaviour in the instructions, where every harness reads it.

### CP007 - Undocumented on a target harness

**Why it matters.** The harness's official documentation does not say what it does with this. That is a gap in the documentation, not a defect in the skill, and it is reported as unknown rather than as an incompatibility.

**Fix.** Try it once on that harness and record what happened, or avoid relying on it.

### CP008 - Target harness refuses the skill

**Why it matters.** A rule the harness publishes is broken - a required field missing, a documented ceiling exceeded, a name that has to match its folder and does not.

**Fix.** Fix the field, or drop the harness from the targets.

### CP009 - No adapter for the named harness

**Why it matters.** The target is not in the registry, so nothing about it was checked. Silence here would read as a pass.

**Fix.** Use a name from `sqs.py harnesses`, or add an adapter under `scripts/harnesses/`.

## evals (EVxxx)

Whether anything would notice the skill breaking: the eval files, the routing invariants, and the agent runs behind `sqs.py eval`.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `EV001` | error | high | low | no | Routing invariant broken |
| `EV002` | info | high | low | no | Skill has no routing cases |
| `EV003` | error | high | low | no | Live routing run below threshold |
| `EV004` | warning | high | low | no | Malformed evals |
| `EV005` | info | high | low | no | Thin trigger set |
| `EV006` | info | high | low | no | Routing runner in the tree was not executed |

### EV001 - Routing invariant broken

**Why it matters.** Two skills claim the same wording and only one can win, or a skill claims wording its own instructions do not serve.

**Fix.** Run `run_evals.py` for the detail; the fix is in the descriptions, not here.

### EV002 - Skill has no routing cases

**Why it matters.** Nothing verifies that this skill fires on the wording a human would actually use, so every description edit moves it blind.

**Fix.** Add cases to `evals/cases/`.

### EV003 - Live routing run below threshold

**Why it matters.** The judge sent wording to the wrong skill.

**Fix.** Read the failing cases: usually one description is claiming a neighbour's branch.

### EV004 - Malformed evals

**Why it matters.** An `evals/` directory that does not parse is worse than none: it looks like the skill is tested.

**Fix.** `evals/evals.json` is `{skill_name, evals: [{id, prompt, expected_output, assertions}]}`; `evals/eval_queries.json` is `[{query, should_trigger}]`.

### EV005 - Thin trigger set

**Why it matters.** A trigger set with few cases on one side measures almost nothing. The negatives matter most, and the useful ones are near-misses: queries sharing keywords with the skill that need something else.

**Fix.** Aim for about twenty queries, eight to ten on each side.

### EV006 - Routing runner in the tree was not executed

**Why it matters.** The routing report comes from a script that lives in the tree being checked. Running it would mean executing code out of the directory the suite was handed to read, which is the thing reading it was meant to avoid.

**Fix.** If the tree is yours, pass `--trust-target`. If it is not, a missing routing report is the correct outcome.

## publish (PBxxx)

What has to be true before the skill leaves the machine it was written on. Half of these are correct-and-intended for a skill that stays home.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `PB001` | warning | high | low | no | No license |
| `PB002` | info | high | low | no | No README |
| `PB003` | error | high | low | no | Personal path in a skill about to be published |
| `PB004` | warning | high | medium | no | Text not in the declared publication language |
| `PB005` | warning | high | low | no | Version drift |
| `PB006` | info | medium | medium | no | Private material in a skill about to be published |

### PB001 - No license

**Why it matters.** Without one, nobody may legally reuse the skill.

**Fix.** Add a `license:` field, or a LICENSE file beside the skill.

### PB002 - No README

**Why it matters.** The SKILL.md talks to the agent. A human deciding whether to install needs a page that talks to them.

**Fix.** Add README.md next to the skill folder.

### PB003 - Personal path in a skill about to be published

**Why it matters.** It leaks your directory layout and breaks on every other machine.

**Fix.** Replace with `~`, or with a path the reader supplies.

### PB004 - Text not in the declared publication language

**Why it matters.** A published skill documented in another language than its repository is read by nobody it was published for.

**Fix.** Translate, or drop `--lang`.

### PB005 - Version drift

**Why it matters.** The version in the plugin or marketplace manifest disagrees with the skill's own.

**Fix.** Make them agree; the manifest is usually the one that went stale.

### PB006 - Private material in a skill about to be published

**Why it matters.** A path into a personal vault, a private repository name, an email address.

**Fix.** Strip it, or keep the skill unpublished.

## quality (QLxxx)

Whether the instructions read like instructions. The module with the most room to lie, so every rule here names a file and a line.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `QL001` | warning | high | low | no | Description too short to carry triggers |
| `QL002` | warning | medium | medium | no | Description states what, never when |
| `QL003` | warning | low | high | no | One branch written twice |
| `QL004` | info | low | high | no | Description and body barely overlap |
| `QL005` | warning | medium | high | no | Steering by prohibition |
| `QL006` | info | medium | medium | no | Unverifiable instruction |
| `QL007` | warning | high | low | no | TODO or placeholder left in the text |
| `QL008` | info | medium | medium | no | Disclaimer naming a neighbouring skill's topic |
| `QL009` | info | high | medium | no | Description repeats the skill's own name |
| `QL010` | warning | medium | low | no | Description talks about itself |
| `QL011` | error | medium | medium | no | Bundled script waits for input |
| `QL012` | info | high | low | no | Unpinned one-off command |
| `QL013` | info | medium | medium | no | Description rules out a topic |

### QL001 - Description too short to carry triggers

**Why it matters.** Trigger conditions are the only thing the agent uses to decide whether to open the skill. Two words hold none.

**Fix.** State what the skill is and list the distinct branches that should reach it.

### QL002 - Description states what, never when

**Why it matters.** A pointer does two jobs: name the material and list the branches that trigger reaching it. This one does the first only, so firing is left to chance.

**Fix.** Add the trigger conditions: `Use when ...`, `Trigger on ...`.

### QL003 - One branch written twice

**Why it matters.** Two trigger phrases that name the same branch cost context on every turn and sharpen nothing. The rule prints the pair, so you can judge it.

**Fix.** Collapse the pair into one trigger. Deliberately high-precision and therefore low-recall: it catches word-level repetition only, and the judgement-level pass over a description lives in `references/writing-rubric.md`.

### QL004 - Description and body barely overlap

**Why it matters.** The skill fires on wording that its own instructions never mention, which usually means the description drifted from what the skill grew into.

**Fix.** Re-read the body and rewrite the description from it.

### QL005 - Steering by prohibition

**Why it matters.** A ban drags the forbidden behaviour into context and makes it more available, not less. The negation is a weak modifier that the strongly-activated concept overruns, so the ban half-reads as an instruction to do the thing.

**Fix.** State the target behaviour positively, so the banned one is never spoken. Keep a prohibition only as a hard guardrail you cannot phrase positively - and pair it with the positive target.

### QL006 - Unverifiable instruction

**Why it matters.** `be thorough`, `write clearly`, `as needed`: the agent cannot tell done from not-done, and neither can you.

**Fix.** Replace with a checkable bound, or with a `bad -> good` pair the reader can compare.

### QL007 - TODO or placeholder left in the text

**Why it matters.** The agent reads it as instruction, not as a note to self.

**Fix.** Finish the sentence or cut it.

### QL008 - Disclaimer naming a neighbouring skill's topic

**Why it matters.** A description is a shop window, not a fence. Naming a neighbour's topic makes this skill a candidate for it; negation does not reliably reverse that.

**Fix.** Move the boundary into the body, which is read after activation and does not affect the choice. Keep it in the description only when the neighbour can fire on its own.

### QL009 - Description repeats the skill's own name

**Why it matters.** Identity the body already carries, paid for on every turn.

**Fix.** Cut the restatement and spend the room on a trigger.

### QL010 - Description talks about itself

**Why it matters.** The description is an instruction to the agent about when to act, not a paragraph about the skill. `Use when the user ...` outperforms `This skill does ...`, and first or second person (`I can help you ...`) is injected into a system prompt where the point of view does not fit.

**Fix.** Rewrite in the imperative, third person: what it does, then the situations that should reach it.

### QL011 - Bundled script waits for input

**Why it matters.** Agents run in non-interactive shells. A script that blocks on a prompt does not fail - it hangs until something kills it, and the skill looks broken for reasons nothing explains.

**Fix.** Take every input from flags, environment variables or stdin, and fail with a message naming the missing one.

### QL012 - Unpinned one-off command

**Why it matters.** `npx eslint` resolves to whatever is newest today. The skill's behaviour then changes without the skill changing, which is the hardest kind of drift to trace.

**Fix.** Pin the version: `npx eslint@9.0.0`.

### QL013 - Description rules out a topic

**Why it matters.** A description is matched on topic, and a negation does not reverse a match. Spelling out the work the skill will not take puts that work's vocabulary in the one place the router reads, so the clause meant as a fence reads as one more reason to fire. `QL008` is this same trap when a neighbouring skill is named; this is the version with only a topic in it, which is the common one.

**Fix.** Move the boundary into the body, which is read after the skill has already been chosen. Keep it in the description only when a model-invoked neighbour would otherwise take the work, and then name that neighbour rather than its topic.

## security (SExxx)

What an installed skill can do to the machine that loads it. A skill is executable text, and an installed skill is a supply chain.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `SE001` | error | high | medium | no | Secret in the skill text |
| `SE002` | warning | medium | medium | no | Destructive command in an instruction |
| `SE003` | error | medium | medium | no | Instruction-override text |
| `SE004` | error | high | low | yes | Hidden or bidirectional Unicode |
| `SE005` | warning | medium | medium | no | Outbound network call carrying local data |
| `SE006` | info | high | medium | no | Absolute path naming a user account |

### SE001 - Secret in the skill text

**Why it matters.** An API key, token or private key committed into a skill travels with every copy of it.

**Fix.** Revoke the credential, then remove it and read it from the environment.

### SE002 - Destructive command in an instruction

**Why it matters.** A recursive delete, a download piped into a shell, world-writable permissions, a force push: the agent runs what the skill tells it to run.

**Fix.** Narrow the command, or make the step ask the human first.

### SE003 - Instruction-override text

**Why it matters.** An instruction to disregard what came before, a claimed change of role, an order to keep something from the user. In a skill you wrote it is a mistake; in a skill you installed it is the payload.

**Fix.** Remove it. If the skill came from elsewhere, read the whole file before using it.

### SE004 - Hidden or bidirectional Unicode

**Why it matters.** Zero-width and bidi control characters make the rendered text differ from the text the model reads - the Trojan Source trick.

**Fix.** Strip the characters. `sqs.py fix` removes them.

### SE005 - Outbound network call carrying local data

**Why it matters.** A step that posts file contents to a host is exfiltration whether or not it was meant as one.

**Fix.** Confirm the host is yours and that the human agreed to the send.

### SE006 - Absolute path naming a user account

**Why it matters.** `C:\Users\<name>`, `/home/<name>`: the skill only works on one machine, and it publishes whose machine that is.

**Fix.** Use `~`, or an environment variable.

## spec (SPxxx)

Agent Skills specification conformance. Everything here is something a skill survives locally and fails on publication, which is why it is a separate pass: the author never sees it until it is too late.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `SP001` | error | high | low | no | No frontmatter |
| `SP002` | error | high | low | yes | Required field `name` missing |
| `SP003` | error | high | low | no | Required field `description` missing |
| `SP004` | error | high | low | yes | `name` does not match the folder |
| `SP005` | warning | high | low | no | `name` longer than 64 characters |
| `SP006` | warning | high | low | yes | `name` off-spec |
| `SP007` | error | high | low | no | `description` empty |
| `SP008` | warning | high | low | no | `description` over 1024 characters |
| `SP009` | warning | high | low | no | `compatibility` over 500 characters |
| `SP010` | warning | high | medium | no | Unknown frontmatter key |
| `SP011` | error | high | low | no | Unclosed code fence |
| `SP012` | info | high | medium | no | Directory outside the conventions |
| `SP013` | info | high | high | no | Extraneous file at the skill root |
| `SP014` | error | high | low | no | No body |
| `SP015` | info | high | low | no | Multiline description |
| `SP016` | warning | high | medium | no | Oversized or binary asset |
| `SP017` | warning | high | medium | no | Reference nested too deep |
| `SP018` | error | high | low | no | XML tag in `name` or `description` |
| `SP019` | error | high | low | no | Reserved word in `name` |

### SP001 - No frontmatter

**Why it matters.** Without a `---` block the file is plain markdown and never loads as a skill.

**Fix.** Open the file with `---`, put `name` and `description` inside, close with `---`.

### SP002 - Required field `name` missing

**Why it matters.** The loader keys the skill by `name`; without it there is nothing to invoke.

**Fix.** Add `name: <folder-name>`.

### SP003 - Required field `description` missing

**Why it matters.** The description is the skill's only context pointer. No description, no triggering.

**Fix.** Add a description stating what the skill does and when to reach for it.

### SP004 - `name` does not match the folder

**Why it matters.** The specification requires them equal. What actually happens is split, and the client-implementation guide states both halves: a lenient client warns and loads the skill anyway, while `skills-ref validate` and publication reject it. So it works on your machine and fails the moment the skill leaves it.

**Fix.** Rename the folder or the field so the two agree.

### SP005 - `name` longer than 64 characters

**Why it matters.** Over the spec limit. Claude Code tolerates it, `skills-ref validate` and publication do not.

**Fix.** Shorten the name.

### SP006 - `name` off-spec

**Why it matters.** The spec allows lowercase latin letters, digits and single inner hyphens only.

**Fix.** Rewrite as `lower-case-with-hyphens`.

### SP007 - `description` empty

**Why it matters.** An empty description is the same as none: the skill never triggers.

**Fix.** Write the description.

### SP008 - `description` over 1024 characters

**Why it matters.** Over the spec limit. It works locally and is rejected on publication.

**Fix.** Cut restated identity and synonym triggers; keep one trigger per branch.

### SP009 - `compatibility` over 500 characters

**Why it matters.** Over the spec limit.

**Fix.** Shorten it.

### SP010 - Unknown frontmatter key

**Why it matters.** A typo in a key is not a syntax error: the field silently vanishes along with its meaning, and the skill keeps loading as if nothing happened.

**Fix.** Fix the spelling, or drop the key.

### SP011 - Unclosed code fence

**Why it matters.** Everything after the unclosed fence reads as code, so the instructions that follow it stop being instructions.

**Fix.** Close the fence.

### SP012 - Directory outside the conventions

**Why it matters.** The specification permits any directory beside SKILL.md and calls `scripts/`, `references/` and `assets/` recommendations, so this is not a violation. It is a portability note: tools that walk the conventional layout will not see the directory, and harnesses document different sets - Cline names `docs/` and `templates/`, Antigravity names `examples/` and `resources/`.

**Fix.** Leave it if the skill links it from SKILL.md; `compat` says what each harness makes of it. Add the name to `allow_dirs` to stop being told.

### SP013 - Extraneous file at the skill root

**Why it matters.** README.md, CHANGELOG.md, Makefile and friends are repository furniture. Inside a published skill they ship as payload nobody reads.

**Fix.** Keep them outside the skill folder, or accept the cost knowingly.

### SP014 - No body

**Why it matters.** Frontmatter present, instructions absent: the skill activates and says nothing.

**Fix.** Write the instructions.

### SP015 - Multiline description

**Why it matters.** A folded or literal block scalar (`>-`, `|`) is valid YAML and Claude Code reads it. It is flagged as non-portable by the vendor-neutral validators, which is as far as the evidence goes: whether a given other runtime reads it has to be tried, not assumed.

**Fix.** Put the description on one line if the skill has to be portable; leave it if Claude Code is the only target.

### SP016 - Oversized or binary asset

**Why it matters.** Every byte in the skill folder travels with it. A large binary is paid for by everyone who installs the skill and read by nobody.

**Fix.** Host it elsewhere and link, or shrink it.

### SP017 - Reference nested too deep

**Why it matters.** Progressive disclosure is one hop: SKILL.md names the file. Three levels down, nothing reaches the file but a reader who already knew it was there.

**Fix.** Flatten to `references/<file>.md`, or link the intermediate level explicitly.

### SP018 - XML tag in `name` or `description`

**Why it matters.** The Agent Skills validation rules forbid XML tags in either field. The description is injected into a system prompt, where a stray tag changes how the surrounding text is read.

**Fix.** Remove the angle brackets; name the tag in prose instead.

### SP019 - Reserved word in `name`

**Why it matters.** `anthropic` and `claude` are reserved in a skill name by the validation rules, and the refusal comes at upload time, after the skill is finished.

**Fix.** Rename the skill after what it does.

## structure (STxxx)

Links, orphans, budgets, section pointers, outbound paths. This is the class of defect that breaks **today, in silence**: a broken pointer into `references/` crashes nothing, the agent just skips the step.

| Code | Severity | Confidence | False positives | Autofix | What it is |
|---|---|---|---|---|---|
| `ST001` | error | high | low | no | Link to a file that does not exist |
| `ST002` | error | high | low | no | Broken sibling link |
| `ST003` | error | high | low | no | Link to a skill that does not exist |
| `ST004` | error | high | low | no | Linked skill has no such file |
| `ST005` | warning | high | medium | no | Orphan file |
| `ST006` | warning | high | low | no | SKILL.md over budget |
| `ST007` | warning | high | low | no | Reference over budget |
| `ST008` | warning | medium | medium | no | Pointer to a section that is gone |
| `ST009` | warning | medium | medium | no | Path naming no folder in this skill |
| `ST010` | warning | medium | medium | no | Path with no skill name |
| `ST011` | error | high | low | no | Outbound path does not exist |
| `ST012` | warning | medium | high | no | Code as prose |
| `ST013` | warning | high | low | no | Body nearly empty |
| `ST014` | error | high | low | no | Duplicate `name` across skills |
| `ST015` | error | high | low | no | No SKILL.md |
| `ST016` | warning | unrated | unrated | no | External link unreachable |

### ST001 - Link to a file that does not exist

**Why it matters.** The agent does not crash on a broken pointer: it skips the step in silence, and the only symptom is that the work came out worse than usual.

**Fix.** Restore the file or correct the link.

### ST002 - Broken sibling link

**Why it matters.** Same silent skip, in the short `[text](neighbour.md)` form that a path-shaped check cannot see.

**Fix.** Restore the file or correct the link.

### ST003 - Link to a skill that does not exist

**Why it matters.** A rename elsewhere left this pointer aimed at nothing.

**Fix.** Point at the new name, or drop the link.

### ST004 - Linked skill has no such file

**Why it matters.** The skill is there, the file inside it is not.

**Fix.** Correct the path, or restore the file in the other skill.

### ST005 - Orphan file

**Why it matters.** Nothing links to it. Either it was never wired up, or it is no longer needed. An orphan is a pruning candidate, not a defect.

**Fix.** Link it from SKILL.md, or delete it.

### ST006 - SKILL.md over budget

**Why it matters.** SKILL.md is loaded whole on every activation, so its length is a permanent tax on the context window.

**Fix.** Move what only some branches need into `references/`, and keep the routing - which reference to open when - in SKILL.md.

### ST007 - Reference over budget

**Why it matters.** A reference is opened whole too, just later. A 30 KB file cancels the point of two-stage loading.

**Fix.** Split it by branch.

### ST008 - Pointer to a section that is gone

**Why it matters.** The file is in place, so an ordinary link check stays quiet while the agent opens the reference and does not find what it came for. Renaming a heading is enough.

**Fix.** Restore the heading, or correct the pointer - copying the section name from the file verbatim, never from memory.

### ST009 - Path naming no folder in this skill

**Why it matters.** Almost always an example path from somebody else's repository rather than a route of your own.

**Fix.** Leave it if it is an example; fix it if it was meant to be a route.

### ST010 - Path with no skill name

**Why it matters.** The file lives in a different skill, and the bare relative path resolves there only by accident.

**Fix.** Spell out `~/.claude/skills/<skill>/<path>`.

### ST011 - Outbound path does not exist

**Why it matters.** A skill that reads a note before working degrades silently when the note is renamed: the theory is gone and the work goes on anyway.

**Fix.** Update the path. Cyrillic is compared NFC/NFD-insensitively, so a hit here is a real miss, not a normalisation artefact.

### ST012 - Code as prose

**Why it matters.** A long executable block sitting in the text is retyped by the model every run - probabilistically, and for tokens. It cannot be run, and it cannot be fixed once.

**Fix.** Move it into `scripts/`; leave the call and how to read its output in the text.

### ST013 - Body nearly empty

**Why it matters.** Enough text to load, not enough to act on.

**Fix.** Write the instructions.

### ST014 - Duplicate `name` across skills

**Why it matters.** One shadows the other and which one wins is not knowable in advance.

**Fix.** Rename one of them.

### ST015 - No SKILL.md

**Why it matters.** The folder is in the skills tree and holds no skill.

**Fix.** Add SKILL.md, or move the folder out.

### ST016 - External link unreachable

**Why it matters.** A dead URL in a reference sends the agent to fetch nothing.

**Fix.** Update or remove the link. 403 is reported separately: it usually means the site blocks bots, not that the page is gone.
