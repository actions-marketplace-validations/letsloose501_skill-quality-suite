{
  "note": "A task set that would spend money measuring nothing. `totals-vague` has no assertion and no output, so no run of it can pass or fail. `totals-draft` still opens with the placeholder `cases --generate` writes, `totals-april` names a fixture that is not in the skill, `totals-pattern` carries a regex the grader would raise on after both arms had run, and `totals-sheet` checks the text inside an .xlsx, which no run can pass. `totals-ok` is the silent side: graded by what `totals.md` contains, with a fixture that exists - and every output under `outputs`, so `EV011` must stay quiet.",
  "command": "evals",
  "expect": [
    "EV008",
    "EV009"
  ],
  "reject": [
    "EV011"
  ]
}
