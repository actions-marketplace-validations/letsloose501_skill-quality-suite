---
name: claude-reserved
description: Converts a <table> element into a markdown table. Use when HTML is pasted in, or when the user asks for a markdown version of a table.
---

# Claude reserved

1. Parse the HTML.
2. Emit the markdown table. Stop when every row is accounted for.
