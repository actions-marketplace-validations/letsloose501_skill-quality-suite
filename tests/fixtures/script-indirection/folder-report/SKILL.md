---
name: folder-report
description: Lists what is in a folder and how big it is. Use when the user asks what is taking up space in a directory.
---

# Folder report

1. Run `scripts/report.py` on the folder the user named.
2. If the folder is packed, run `scripts/unpack.py` or `scripts/unpack.sh` first.
3. Print the report.
