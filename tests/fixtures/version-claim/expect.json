{
  "note": "Two skills compared against an earlier copy of the same tree. `receipt-sorter` gained a whole step and still says 0.3.0 - the version nobody can tell apart from the one they installed. `invoice-filer` dropped `Bash` out of `allowed-tools`, which breaks every caller that relied on it, and moved the patch digit as if nothing had happened.",
  "command": "publish",
  "args": ["--since", "previous"],
  "expect": [
    "PB010",
    "PB011"
  ]
}
